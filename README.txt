A wrapper around a rewrite of a suite of applications I've built and use on various websites. An attempt to make them more reusable.

In this first release, a simple photo management application, ``bilskirnir``.
